// styles/stylesemergency.ts

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ffeded',
    justifyContent: 'center',
    alignItems: 'center',
  },
  countdown: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#b22222',
  },
  calling: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#ff0000',
  },
});

export default styles;
